#!/usr/bin/env bash
set -eu -o pipefail

readonly repo_dir="$( cd -- $(dirname $(readlink -f ${BASH_SOURCE}))/..; pwd )"

cd "${repo_dir}"

bin/generate-source

cargo doc \
          --workspace \
          --package capnp \
          --document-private-items \
          --no-deps \
          --open
